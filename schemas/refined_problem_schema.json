{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Refined Problem Schema",
  "type": "object",
  "properties": {
    "original_problem": {
      "description": "Cópia do JSON recebido do Meaning Agent.",
      "type": "object"
    },
    "refined_problem": {
      "description": "Problema refinado após análise do Pesquisador Agent.",
      "type": "object",
      "properties": {
        "problem_type": { "type": "string" },
        "sense": { "type": "string", "enum": ["maximize", "minimize"] },
        "objective": { "type": "string" },
        "objective_description": { "type": "string" },
        "decision_variables": { "type": "object" },
        "auxiliary_variables": { "type": "object" },
        "constraints": { "type": "array" },
        "data": { "type": "object" },
        "is_valid_problem": { "type": "boolean" },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
        "clarification": { "type": "string" },
        "business_context": { "type": "object" }
      },
      "required": [
        "problem_type", "sense", "objective", "objective_description", "decision_variables", "auxiliary_variables", "constraints", "data", "is_valid_problem", "confidence", "clarification", "business_context"
      ]
    },
    "improvements": {
      "description": "Lista de melhorias sugeridas pelo Pesquisador Agent.",
      "type": "array",
      "items": { "type": "string" }
    },
    "missing_data": {
      "description": "Lista de dados faltantes identificados.",
      "type": "array",
      "items": { "type": "string" }
    },
    "clarification_requests": {
      "description": "Perguntas ou pontos que precisam de esclarecimento do usuário.",
      "type": "array",
      "items": { "type": "string" }
    }
  },
  "required": ["original_problem", "refined_problem", "improvements"]
} 